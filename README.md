# Punto Switcher для Ubuntu (Linux)

Легковесный и мощный автоматический переключатель раскладки клавиатуры и корректор текста для Linux (Ubuntu 24.04+), вдохновленный оригинальным Punto Switcher. Поддерживает Wayland и X11.

## 🚀 Возможности

### Основные функции

* **Автоматическое переключение раскладки**: Программа анализирует вводимый текст и исправляет раскладку "на лету" (например, `ghbdtn` -> `привет`).
* **Ручная коррекция**:
  * `Pause/Break`: Исправить раскладку последнего набранного слова (или выделенного текста).
  * `Ctrl + Shift + Pause`: Транслитерация текста (выделенного или последнего слова).
  * `Ctrl + Pause`: Инверсия регистра (пРИВЕТ -> Привет).
  * `Alt + Pause`: Преобразование числа в текст (123 -> сто двадцать три).
* **Автозамена (Autoreplace)**:
  * Автоматическая замена сокращений (например, `дд` + Пробел -> `Добрый день!`).
* **Буфер обмена**:
  * Коррекция не только последнего набранного слова, но и выделенного текста через системный буфер обмена.

### Интерфейс и удобство

* **Трей-иконка**: Отображение статуса и быстрый доступ к настройкам.
* **Звуковое сопровождение**: Озвучка переключений и опечаток (щелчки, затвор, бипы).
* **Список исключений**: Возможность отключения программы в определенных окнах (например, в играх или IDE).
* **Конфигурация**: Все настройки хранятся в удобном JSON формате и могут обновляться "на лету".

## 🛠 Технические особенности и Упрощения

Проект написан на Python с использованием современных Linux API (`evdev`, `uinput`).

1. **Детектор языка**:

   * В текущей версии используется эффективный эвристический алгоритм, основанный на анализе сочетаний согласных и запрещенных символов для русского и английского языков.
   * *Упрощение*: Отсутствует полноценная проверка по словарю частотности (биграммы/триграммы), что делает программу очень быстрой, но в редких случаях возможны ложные срабатывания на специфических аббревиатурах.
2. **Ввод/Вывод**:

   * Использует `evdev` для перехвата нажатий на низком уровне.
   * Использует `uinput` для эмуляции нажатий (удаление неправильного слова и печать нового).
   * Работает корректно как в **X11**, так и в **Wayland** (благодаря работе на уровне ядра, а не графического сервера).
3. **Определение окон**:

   * Использует `xdotool` для определения заголовков окон.
   * *Ограничение*: На "чистом" Wayland (без XWayland) определение активного окна может быть ограничено политиками безопасности композитора. В этом случае глобальные исключения могут не срабатывать.

## 📦 Установка и Сборка

### Требования

* Ubuntu 24.04 или совместимая ОС.
* Python 3.10+.
* `sudo` права для установки.

### Сборка пакета (.deb)

Для сборки установочного пакета из исходного кода выполните:

```bash
./scripts/build_package.sh
```

В корне проекта появится файл `punto_ubuntu.deb`.

### Установка

Установите собранный пакет:

```bash
sudo apt install ./punto_ubuntu.deb
```

> **ВАЖНО**: Сразу после установки необходимо сделать **Выход из системы (Logout)** и зайти снова. Это нужно для применения прав доступа вашего пользователя к устройствам ввода (группы `input` и `uinput`).

### Удаление

```bash
sudo apt remove punto-ubuntu
```

## ⚙️ Настройка

Папка конфигурации: `~/.config/punto-switcher/`

1. **autoreplace.json** (Автозамена):

   ```json
   {
       "дд": "Добрый день!",
       "имхо": "по моему скромному мнению"
   }
   ```
2. **exceptions.json** (Исключения):

   ```json
   {
       "window_titles": ["IntelliJ IDEA", "Counter-Strike"]
   }
   ```

После ручного редактирования файлов нажмите кнопку "Сохранить" в окне настроек (через иконку в трее), чтобы применить изменения без перезагрузки.

## 🗺 Roadmap (Планы развития)

* [ ] **Умный словарь**: Внедрение словарей биграмм/триграмм для более точного определения языка и уменьшения ложных срабатываний.
* [ ] **GUI для словарей**: Редактор автозамены и исключений прямо в графическом интерфейсе.
* [ ] **Wayland Native**: Улучшение детекции активного окна в Wayland (через GNOME Shell Extension или Portal API).
* [ ] **Modern UI**: Переход Tray App на PyGObject (Gtk4 + LibAdwaita) для современного внешнего вида.
* [ ] **Hotkeys UI**: Возможность переназначения горячих клавиш через GUI.
* [ ] **Shift + Pause**: Циклическое выравнивание/переключение регистра (ПРИвет -> привет -> ПРИВЕТ).
